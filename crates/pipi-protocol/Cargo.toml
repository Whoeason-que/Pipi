[package]
name = "pipi-protocol"
version = "26.9.3"
description = "Pipi 的持久化与传输协议类型"
edition = "2021"

[dependencies]
pipi-error = { path = "../pipi-error" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["time"] }
